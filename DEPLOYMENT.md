# 공개 포트폴리오 배포

## 구성

- `public-portfolio.html`: 반응형 공개 포털. 업체 필터, 검색, RC 필터, 사진 확대, 프로젝트별 공유 링크, 비교표와 인쇄.
- `portfolio-data.json`: 상위 세 업체 15개 프로젝트와 대표 사진 12장. 원문·역할·구조·한계·사진 크레딧을 분리.
- `overview.html`: 2026-09-15 개편 직전 `index.html`의 원본 보존본. 기존 blob을 그대로 재사용.
- `index.html`: 새 포털로 연결. 기존 조사 생성기 `build.py`를 실행하더라도 Vercel 루트는 `public-portfolio.html`을 읽으므로 새 포털이 사라지지 않음.
- `api/public-site.mjs`, `vercel.json`: 허용된 공개 파일만 읽는 Vercel 전달 계층.

## 연결 방식과 한계

Vercel 프로젝트 이름은 `jeju-proposal`이며 공개 저장소 `kks0488/jeju_proposal`의 **master 원본을 HTTP로 읽는 방식**이다. Vercel의 네이티브 Git Integration(커밋마다 빌드 생성)과는 다르다. 현재 연결 도구에는 프로젝트의 Git 연결을 수정하는 동작이 제공되지 않아 네이티브 연동이 설정됐다고 기록하지 않는다.

포털 HTML·데이터·기존 보고서를 master에 커밋하면 서버/배포 캐시 갱신 뒤 반영된다. 원본/페이지 캐시는 60초, stale-while-revalidate는 60초이고 원격 GitHub 캐시/전파 때문에 더 걸릴 수 있다. 사진 캐시는 1시간이다. 함수 코드와 vercel.json 변경은 별도 재배포가 필요하다. GitHub 원본 장애나 외부 사진 출처 장애 시 해당 자료가 열리지 않을 수 있다.

네이티브 연동으로 바꿀 때에는 Vercel 프로젝트 Settings > Git에서 정확히 이 저장소와 master를 연결하고, 정적 빌드로 전환할지 현재 원본 전달 구성을 유지할지 결정해야 한다. 공개 주소가 이미 동작하는 경우 불필요한 새 프로젝트를 만들지 않는다.

## 공개 범위

허용 목록은 포털·현재 검토 HTML·필수 스타일/스크립트·채점 데이터뿐이다. `history/`, 과거 설계 `images/`, 임의 경로·임의 외부 URL은 제공하지 않는다. 이전 사진집의 사적 휴대전화 패턴은 전달본에서 가리며 GitHub 원본은 수정하지 않는다. 검색엔진에는 noindex/nofollow를 요청하되, 이것은 인증이나 비밀 유지 장치가 아니다.

큰 사진집은 HTML에 내장된 base64 이미지를 별도 이미지 응답으로 분리하여 전달한다. 새 대표 사진은 데이터에 등록된 HTTPS 주소와 허용 호스트만 이용한다. 개인정보·토큰·사용자 쿠키를 원본에 전달하지 않는다.

## 점검

`/health`는 원본 연결, 프로젝트·사진 개수를 반환한다. `/health?images=1`은 등록된 대표 사진의 HTTP/MIME/크기 접근 결과를 점검한다. 원문 자체의 사실 정확성이나 사진의 권리 허락을 자동 보증하는 기능은 아니다.

로컬 DOM 검사는 1440·768·390·320px에서 업체/RC 필터, 검색, 빈 결과, 상세 모달, 이전/다음, Escape, 가로 넘침을 검사했다. 격리 실행 환경 때문에 fetch/history를 모의한 문서 테스트이며, 실배포 HTTP·원격 사진 점검과 구분한다. 배포 완료 및 실제 접근 결과는 후속 검증 기록에 별도로 남긴다.
